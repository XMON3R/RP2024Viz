// vite.config.js
export default {
    build: {
      outDir: 'dist', // Specify the output directory
      emptyOutDir: false, // Do not empty the output directory before building
      // Add any additional build settings as needed
    },
  };